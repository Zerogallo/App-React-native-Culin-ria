import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
  },
  boxCont: {
    marginHorizontal: 50,
  },
  boxImg: {
    width: 100,
    height: 100,
  },

  boxtext: {
    fontSize: 20,
    fontWeight: "bold",
    textAlign: "center",
  },
});
